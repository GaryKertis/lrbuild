Assets on "LR_BUILD.zip" : -Scratch library-Font -Storyboards & image assets. -Video Build an HTML unit that follows the storyboards found on the package. The same unit should serve both landscape & portrait window size/orientation as per storyboards. Scratch library included in the package. All copy should be editable text except for the brand statement "above & beyond". This unit should display consistently across Chrome, Safari, Firefox, IE & ipad devices. 